# Prettier owns the docs site, the Markdown and the workflow YAML. Everything
# below is either not ours to reformat or not text we hand-edit.

# Build and tooling output.
build/
node_modules/
.venv/
package-lock.json

# The Elektron user manual: copyrighted, kept out of the published tree.
temp/

# Generated by packaging/stamp-docs.py, which rewrites them with regexes.
# Prettier is not configured for XML anyway, and llms.txt is plain text, so
# these are listed to make the intent explicit rather than to change behaviour.
docs/sitemap.xml
docs/llms.txt

# GPL text. Not ours to reflow.
LICENSE
