## What this changes

<!-- What it does and why. If it fixes an issue, link it. -->

## How you tested it

<!-- Against real hardware, in a host, or by the test suite alone. Saying "not tested on hardware" is fine and useful, since only one person here has a unit. -->

## Checks

- [ ] `make format-check` passes
- [ ] `make test` passes
- [ ] Touched `core/`, and added a case to `core/tests/tests.cpp`
- [ ] Touched the docs site, and `make links-check` and `make images-check` pass
- [ ] Anything stated as hardware behaviour is something that was actually observed, not inferred

<!-- Leave the boxes that do not apply unticked rather than deleting them. -->
